{
  "name": "kanoonsaathi",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node dist/server.js",
    "start:prod": "cd backend && npx prisma migrate deploy && node dist/server.js"
  }
}
